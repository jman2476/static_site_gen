def copy_directory_contents(source, destination):
    #check that source, destination are valid paths

    #if directory, identify sub-directories and files
    ## copy files to destination
    ## call copy_directory_contents on sub_directories
